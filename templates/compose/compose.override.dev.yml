services:
  challenge-api:
    image: redteamsubnet61/historical_fingerprinter:latest
    volumes:
      - "./src/hfp_challenge/challenge/scripts/docker-entrypoint.sh:/usr/local/bin/docker-entrypoint.sh"
      - "./src/hfp_challenge/challenge/api:/app/historical_fingerprinter/api"
      - "./volumes/.vscode-server:/home/hfp-user/.vscode-server"
    # deploy:
    #   replicas: 3
    #   resources:
    #     limits:
    #       cpus: "0.5"
    #       memory: 512M
    # ports: !override
    #   - "10001-10003:${HFP_CHALLENGE_API_PORT:-10001}"
    # command: ["/bin/bash"]
    # command: ["-b", "uvicorn api.main:app --host=0.0.0.0 --port=${HFP_CHALLENGE_API_PORT:-10001} --no-access-log --no-server-header --proxy-headers --forwarded-allow-ips='*' --reload"]
    # healthcheck:
    #   test: ["CMD-SHELL", "curl -f http://localhost:${HFP_CHALLENGE_API_PORT:-10001}/ping"]
    #   start_period: 30s
    #   start_interval: 1s
    #   interval: 5m
    #   timeout: 5s
    #   retries: 3

  miner-commit-api:
    image: myhub/rest-hfp-commit:latest
    build:
      context: ./examples/miner_commit
    restart: unless-stopped
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-UTC}
    env_file:
      - path: .env
        required: false
    volumes:
      - "./examples/miner_commit/src:/app"
    ports:
      - ${HFP_CHALLENGE_MINER_COMMIT_API_PORT:-10002}:${HFP_CHALLENGE_MINER_COMMIT_API_PORT:-10002}
    command: ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "${HFP_CHALLENGE_MINER_COMMIT_API_PORT:-10002}", "--reload"]
    tty: true
